#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string str;
    cin >> str;

    int l1 = 0;
    int f0 = str.length() - 1;

    for (int i = n-1; i >= 0)
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int t;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}